# 标签治理(子项目 H)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 182 个去重标签精简到约 40–60(合并重复 + 归并碎标签),并将 `/tags/` 总览页左侧改为频次标签云、优化文章内 badge 观感。

**Architecture:** Python 脚本 `retag.py`(audit + 按 `tag-map.json` 批量改写 tags 行,dry-run→apply,纯 `re` 不引入 YAML 依赖,与现有 `compress_images.py` 一致)处理数据;Hugo 服务端模板 `terms.html` 覆盖渲染频次云;`custom.css` 纯 CSS 控样式。

**Tech Stack:** Python 3.13(标准库 `os`/`re`/`json`/`argparse`)、pytest(测纯函数)、Hugo extended、Go template、CSS。

## Global Constraints

- 只治 `tags`,**不动** `categories`/`title`/`description`/正文。
- front matter 为 YAML,`tags:` 行均为**单行** `tags: ["A", "B"]`(已核实 68/68 篇);改写只匹配首个 `---`…`---` 块内的 `tags:` 行。
- 文件保持 **LF 行尾**(CRLF 致 Typora/YAML 问题,见 CLAUDE.md)。
- 不改 `themes/dream-3.13.0/` 源码,仅在 `layouts/` 覆盖。
- Tailwind 预编译铁律:新 utility 类在预编译 `output.css` 不存在则失效;字号用**内联 style**(动态值),容器/档样式用 `custom.css` 纯 CSS(稳定类名)。
- 目标标签数:182 → 约 40–60。
- **不提交** `content/posts/3dgenai/`(未跟踪,与本项目无关)。
- 分支:建议新建 `feature/tags`(基于当前 `feature/code-blocks` 或 main 合并后)。改写前确认 `git status` 工作区干净(除 `3dgenai/`)。

---

## 文件结构

| 文件 | 责任 | 动作 |
|------|------|------|
| `retag.py` | 标签审计 + 按映射批量改写 | Create |
| `tag-map.json` | 旧→新映射 + 删除列表(分领域审定) | Create |
| `tests/test_retag.py` | 测 retag 纯函数(套映射+去重) | Create |
| `content/posts/**/*.md` | 68 篇 tags 行改写(经脚本) | Modify(脚本) |
| `layouts/_default/terms.html` | 左侧标签列表→频次云,保留右网格 | Create(覆盖主题) |
| `static/css/custom.css` | `.tag-cloud*` + 文章内 badge 样式 | Modify |

---

## Task 1: `retag.py` 核心改写逻辑(纯函数 + 测试)

先做可单测的纯函数:给定标签列表 + 映射,产出新标签列表(套 rename、去 delete、去重保序)。审计/IO 在 Task 2、3。

**Files:**
- Create: `retag.py`
- Test: `tests/test_retag.py`

**Interfaces:**
- Produces:
  - `apply_tag_map(tags: list[str], rename: dict[str,str], delete: list[str]) -> list[str]` —— 对单篇标签列表套映射:命中 `rename` 换值、在 `delete` 移除、未命中保留,最后去重保序。
  - `rewrite_tags_line(line: str, rename, delete) -> str` —— 输入一行 `tags: [...]`,解析其中带引号标签,套 `apply_tag_map`,重写为 `tags: ["A", "B"]` 形式(保持前导 `tags: `,双引号,逗号空格分隔)。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_retag.py
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(__file__)))
from retag import apply_tag_map, rewrite_tags_line


def test_apply_rename_and_merge():
    # c++ 与 C++ 合并到 C++;扩散模型/文生图 合并到 Diffusion
    tags = ["c++", "C++", "扩散模型", "文生图", "语法"]
    rename = {"c++": "C++", "扩散模型": "Diffusion", "文生图": "Diffusion"}
    delete = []
    assert apply_tag_map(tags, rename, delete) == ["C++", "Diffusion", "语法"]


def test_apply_delete_and_dedupe():
    tags = ["入门", "环境配置", "LLM", "LLM"]
    rename = {}
    delete = ["入门", "环境配置"]
    assert apply_tag_map(tags, rename, delete) == ["LLM"]


def test_unmapped_kept_in_order():
    tags = ["A", "B", "C"]
    assert apply_tag_map(tags, {}, []) == ["A", "B", "C"]


def test_rewrite_tags_line_basic():
    line = 'tags: ["排序算法", "冒泡排序", "插入排序"]'
    rename = {"冒泡排序": "排序算法", "插入排序": "排序算法"}
    out = rewrite_tags_line(line, rename, [])
    assert out == 'tags: ["排序算法"]'


def test_rewrite_preserves_single_quotes_input():
    # 兼容单引号或空格变体输入,输出统一双引号
    line = "tags: ['LLM', 'NLP']"
    out = rewrite_tags_line(line, {}, [])
    assert out == 'tags: ["LLM", "NLP"]'


def test_rewrite_empty_after_delete():
    line = 'tags: ["入门"]'
    out = rewrite_tags_line(line, {}, ["入门"])
    assert out == 'tags: []'
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 -m pytest tests/test_retag.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'retag'` 或 `ImportError`。

- [ ] **Step 3: 写最小实现**

```python
# retag.py
import os
import re
import json
import argparse

TARGET_DIR = "./content/posts/"


def apply_tag_map(tags, rename, delete):
    """套映射:rename 换值,delete 移除,未命中保留,去重保序。"""
    delete_set = set(delete)
    out = []
    seen = set()
    for t in tags:
        if t in delete_set:
            continue
        new = rename.get(t, t)
        if new in delete_set:
            continue
        if new not in seen:
            seen.add(new)
            out.append(new)
    return out


def _parse_tags(line):
    """从 `tags: [...]` 行提取标签(支持单/双引号)。"""
    inside = line.split("[", 1)[1].rsplit("]", 1)[0]
    return re.findall(r"""['"]([^'"]*)['"]""", inside)


def rewrite_tags_line(line, rename, delete):
    """重写单行 tags,输出统一双引号、逗号空格分隔。"""
    indent = line[: len(line) - len(line.lstrip())]
    tags = _parse_tags(line)
    new_tags = apply_tag_map(tags, rename, delete)
    body = ", ".join('"%s"' % t for t in new_tags)
    return "%stags: [%s]" % (indent, body)
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 -m pytest tests/test_retag.py -v`
Expected: PASS(6 passed)。

- [ ] **Step 5: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add retag.py tests/test_retag.py
git commit -m "feat: retag.py 标签映射纯函数(套映射/去重)+ 测试"
```

---

## Task 2: `retag.py` audit 模式(按领域聚合标签)

加 audit:扫全部文章,聚合「标签 → 频次 → 所属文章」,按顶层领域目录分组打印,供产出 `tag-map.json` 草案。

**Files:**
- Modify: `retag.py`
- Test: `tests/test_retag.py`

**Interfaces:**
- Consumes: `apply_tag_map`, `rewrite_tags_line`(Task 1)。
- Produces:
  - `find_tags_line(text: str) -> str | None` —— 返回首个 front matter 块(首对 `---`)内的 `tags:` 行原文,无则 None。
  - `audit(target_dir=TARGET_DIR) -> dict` —— 返回 `{tag: {"count": int, "files": [relpath...]}}`。
  - `main()` 支持 `--audit`:调 `audit`,按 `content/posts/<领域>/` 顶层目录分组打印标签+频次+篇数,末尾打印去重标签总数。

- [ ] **Step 1: 写失败测试**

```python
# 追加到 tests/test_retag.py
from retag import find_tags_line


def test_find_tags_line_in_frontmatter():
    text = '---\ntitle: 测试\ntags: ["A", "B"]\ndate: 2025\n---\n正文 tags: 不该匹配\n'
    assert find_tags_line(text) == 'tags: ["A", "B"]'


def test_find_tags_line_none_when_absent():
    text = '---\ntitle: 无标签\n---\n正文\n'
    assert find_tags_line(text) is None


def test_find_tags_line_ignores_body():
    # 正文里出现 tags: 不在首个 --- 块内,不该被当成 front matter tags
    text = '---\ntitle: x\n---\n这里写 tags: ["假"]\n'
    assert find_tags_line(text) is None
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 -m pytest tests/test_retag.py -v -k find_tags_line`
Expected: FAIL —— `ImportError: cannot import name 'find_tags_line'`。

- [ ] **Step 3: 写实现(追加到 retag.py)**

```python
def find_tags_line(text):
    """返回首个 front matter(首对 ---)块内的 tags: 行,无则 None。"""
    if not text.startswith("---"):
        return None
    end = text.find("\n---", 3)
    if end == -1:
        return None
    block = text[3:end]
    for line in block.splitlines():
        if line.lstrip().startswith("tags:") and "[" in line:
            return line
    return None


def _iter_md(target_dir):
    for root, _, files in os.walk(target_dir):
        for fn in files:
            if fn.endswith(".md"):
                yield os.path.join(root, fn)


def audit(target_dir=TARGET_DIR):
    result = {}
    for path in _iter_md(target_dir):
        with open(path, "r", encoding="utf-8") as f:
            text = f.read()
        line = find_tags_line(text)
        if not line:
            continue
        for t in _parse_tags(line):
            entry = result.setdefault(t, {"count": 0, "files": []})
            entry["count"] += 1
            entry["files"].append(os.path.relpath(path, target_dir))
    return result


def _domain_of(relpath):
    """relpath 形如 '算法刷题/xxx.md' → 顶层领域目录名。"""
    return relpath.split(os.sep, 1)[0]


def _print_audit(target_dir=TARGET_DIR):
    data = audit(target_dir)
    # 按领域分组:领域 → [(tag, count)]
    domains = {}
    for tag, info in data.items():
        dom = _domain_of(info["files"][0])
        domains.setdefault(dom, []).append((tag, info["count"]))
    for dom in sorted(domains):
        print("\n# 领域:%s" % dom)
        for tag, cnt in sorted(domains[dom], key=lambda x: -x[1]):
            print("  %3d  %s" % (cnt, tag))
    print("\n=== 去重标签总数:%d ===" % len(data))


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--audit", action="store_true", help="审计标签,按领域分组打印")
    args = ap.parse_args()
    if args.audit:
        _print_audit()


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 -m pytest tests/test_retag.py -v`
Expected: PASS(9 passed)。

- [ ] **Step 5: 跑 audit 看真实分布**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 retag.py --audit`
Expected: 按领域分组打印标签+频次,末尾 `去重标签总数:182`。

- [ ] **Step 6: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add retag.py tests/test_retag.py
git commit -m "feat: retag.py --audit 按领域聚合标签频次"
```

---

## Task 3: 产出并审定 `tag-map.json`(分领域逐批,人工交互)

**这是人工决策任务,不是纯代码。** 实施者(或主线程)据 `--audit` 输出,按领域逐批产出「旧→新」映射草案给用户审,合成最终 `tag-map.json`。

**Files:**
- Create: `tag-map.json`

- [ ] **Step 1: 跑 audit 取分领域清单**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 retag.py --audit > /tmp/tag-audit.txt && cat /tmp/tag-audit.txt`

- [ ] **Step 2: 逐领域产出映射草案,提交用户审**

对每个领域(3dcomputervison/3dgenai、agent/llm、ai编程/comfyui、c++/侯捷面向对象、games101笔记/虚幻引擎/游戏开发/multiplayercourse、机器学习、算法刷题、课堂笔记/个人感悟/hexo博客),列出该领域出现的标签,给出建议:
- 大小写/同义合并(如 `c++`→`C++`、`扩散模型`/`文生图`/`AI绘画`→`Diffusion`)。
- 碎标签(频次=1)归并到上级主题词(如 `冒泡排序`/`插入排序`→`排序算法`)或加入 `delete`。
- 保留的主标签(目标全站 40–60)。

用 AskUserQuestion 或逐批确认。**用户审定后**才写入文件。

- [ ] **Step 3: 写最终 `tag-map.json`**

格式(示例,实际内容据审定结果):

```json
{
  "rename": {
    "c++": "C++",
    "扩散模型": "Diffusion",
    "文生图": "Diffusion",
    "AI绘画": "Diffusion",
    "冒泡排序": "排序算法",
    "插入排序": "排序算法",
    "面相对象": "面向对象"
  },
  "delete": ["入门", "环境配置"]
}
```

- [ ] **Step 4: 校验 JSON 合法 + 预估结果**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 -c "import json; m=json.load(open('tag-map.json')); print('rename:', len(m['rename']), 'delete:', len(m['delete']))"`
Expected: 打印 rename/delete 条数,无 JSON 报错。

- [ ] **Step 5: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add tag-map.json
git commit -m "feat: tag-map.json 标签归并映射表(分领域审定)"
```

---

## Task 4: `retag.py` 改写模式(dry-run → apply)

加批量改写:读 `tag-map.json`,逐篇套映射重写 tags 行。默认 dry-run 打印 diff,`--apply` 落盘。

**Files:**
- Modify: `retag.py`
- Test: `tests/test_retag.py`

**Interfaces:**
- Consumes: `find_tags_line`, `rewrite_tags_line`(Task 1/2);`tag-map.json`(Task 3)。
- Produces:
  - `process_file(path, rename, delete, apply=False) -> tuple[str,str] | None` —— 返回 `(old_line, new_line)` 若有变化,否则 None;`apply=True` 时写回(仅替换 tags 行,保留其余字节,LF 行尾)。
  - `main()` 增 `--apply`:无 `--apply` 为 dry-run 打印每篇 old→new;有则落盘并统计。

- [ ] **Step 1: 写失败测试**

```python
# 追加到 tests/test_retag.py
import tempfile
from retag import process_file


def test_process_file_dryrun_no_write():
    content = '---\ntitle: t\ntags: ["c++", "语法"]\ndate: 2025\n---\n正文\n'
    with tempfile.NamedTemporaryFile("w", suffix=".md", delete=False, encoding="utf-8") as f:
        f.write(content)
        path = f.name
    try:
        old, new = process_file(path, {"c++": "C++"}, [], apply=False)
        assert old == 'tags: ["c++", "语法"]'
        assert new == 'tags: ["C++", "语法"]'
        # dry-run 不写
        assert open(path, encoding="utf-8").read() == content
    finally:
        os.unlink(path)


def test_process_file_apply_writes():
    content = '---\ntitle: t\ntags: ["c++", "语法"]\n---\n正文\n'
    with tempfile.NamedTemporaryFile("w", suffix=".md", delete=False, encoding="utf-8") as f:
        f.write(content)
        path = f.name
    try:
        process_file(path, {"c++": "C++"}, [], apply=True)
        after = open(path, encoding="utf-8").read()
        assert 'tags: ["C++", "语法"]' in after
        assert "正文" in after  # 正文未动
        assert "title: t" in after  # 其他 front matter 未动
    finally:
        os.unlink(path)


def test_process_file_no_change_returns_none():
    content = '---\ntags: ["LLM"]\n---\n'
    with tempfile.NamedTemporaryFile("w", suffix=".md", delete=False, encoding="utf-8") as f:
        f.write(content)
        path = f.name
    try:
        assert process_file(path, {}, [], apply=False) is None
    finally:
        os.unlink(path)
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 -m pytest tests/test_retag.py -v -k process_file`
Expected: FAIL —— `ImportError: cannot import name 'process_file'`。

- [ ] **Step 3: 写实现(追加/修改 retag.py)**

```python
def process_file(path, rename, delete, apply=False):
    with open(path, "r", encoding="utf-8") as f:
        text = f.read()
    old_line = find_tags_line(text)
    if not old_line:
        return None
    new_line = rewrite_tags_line(old_line, rename, delete)
    if new_line == old_line:
        return None
    if apply:
        # 只替换首个出现的该行,保留其余内容与 LF
        new_text = text.replace(old_line, new_line, 1)
        with open(path, "w", encoding="utf-8", newline="\n") as f:
            f.write(new_text)
    return (old_line, new_line)
```

替换 `main()` 为:

```python
def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--audit", action="store_true", help="审计标签,按领域分组打印")
    ap.add_argument("--apply", action="store_true", help="落盘改写(默认 dry-run)")
    ap.add_argument("--map", default="tag-map.json", help="映射表路径")
    args = ap.parse_args()

    if args.audit:
        _print_audit()
        return

    with open(args.map, "r", encoding="utf-8") as f:
        m = json.load(f)
    rename, delete = m.get("rename", {}), m.get("delete", [])

    changed = 0
    for path in _iter_md(TARGET_DIR):
        r = process_file(path, rename, delete, apply=args.apply)
        if r:
            changed += 1
            old, new = r
            print("%s\n  - %s\n  + %s" % (os.path.relpath(path, TARGET_DIR), old, new))
    mode = "已改写" if args.apply else "[dry-run] 将改写"
    print("\n=== %s %d 篇 ===" % (mode, changed))
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 -m pytest tests/test_retag.py -v`
Expected: PASS(12 passed)。

- [ ] **Step 5: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add retag.py tests/test_retag.py
git commit -m "feat: retag.py 批量改写 tags(dry-run/apply)"
```

---

## Task 5: 执行改写(dry-run 审 → apply → 验证)

**Files:**
- Modify: `content/posts/**/*.md`(经脚本)

- [ ] **Step 1: 确认工作区干净**

Run: `cd /Users/ethanhu/Documents/self/my_blog && git status --short`
Expected: 仅 `?? content/posts/3dgenai/`(其余已提交)。如有其他改动先处理。

- [ ] **Step 2: dry-run 审改动**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 retag.py`
Expected: 打印每篇 old→new tags 行 + `[dry-run] 将改写 N 篇`。人工扫一遍合并是否符合预期;若不对,回 Task 3 改 `tag-map.json` 再 dry-run。

- [ ] **Step 3: apply 落盘**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 retag.py --apply`
Expected: `已改写 N 篇`。

- [ ] **Step 4: audit 复核标签数**

Run: `cd /Users/ethanhu/Documents/self/my_blog && python3 retag.py --audit | tail -1`
Expected: `去重标签总数:` 降至约 40–60。

- [ ] **Step 5: git diff 抽查**

Run: `cd /Users/ethanhu/Documents/self/my_blog && git diff --stat content/posts/ && git diff content/posts/算法刷题/ | head -40`
Expected: 仅 `tags:` 行变化;title/description/categories/正文未动;无 CRLF。

- [ ] **Step 6: hugo 构建无错**

Run: `cd /Users/ethanhu/Documents/self/my_blog && rm -rf public && hugo 2>&1 | grep -iE 'error' ; echo done`
Expected: 无 error 行(`.Site.LanguageCode` 既有 WARN 除外)。**勿与 hugo server 并行。**

- [ ] **Step 7: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add content/posts/
git commit -m "refactor: 批量精简文章标签(182→约N,合并重复+归并碎标签)"
```

---

## Task 6: `terms.html` 频次标签云

覆盖主题 terms.html,左侧标签列表改频次云(字号按 `.Count` + 计数),右侧文章网格保留。

**Files:**
- Create: `layouts/_default/terms.html`(覆盖 `themes/dream-3.13.0/layouts/_default/terms.html`)

**Interfaces:**
- Produces:DOM 类名 `.tag-cloud`(容器)、`.tag-cloud__item`(标签链接,内联 `font-size`)、`.tag-cloud__count`(计数),供 Task 7 CSS。

- [ ] **Step 1: 复制主题文件为覆盖基底**

Run: `cd /Users/ethanhu/Documents/self/my_blog && cp themes/dream-3.13.0/layouts/_default/terms.html layouts/_default/terms.html`

- [ ] **Step 2: 替换左侧标签区为频次云**

把 `layouts/_default/terms.html` 中这段(左侧 Alphabetical 平铺):

```go-html-template
    <div class="flex flex-wrap gap-4 mt-6">
      {{ range .Data.Terms.Alphabetical }}
      <a href="{{ .Page.RelPermalink }}" class="badge h-6 no-underline hover:badge-primary">
        {{ .Page.LinkTitle }}
      </a>
      {{ end }}
    </div>
```

替换为(按 ByCount 降序 + 线性插值字号 + 计数;min==max 防除零):

```go-html-template
    <div class="tag-cloud mt-6">
      {{ $terms := .Data.Terms.ByCount }}
      {{ $max := 1 }}{{ $min := 1 }}
      {{ with $terms }}
        {{ $max = (index . 0).Count }}
        {{ $min = (index . (sub (len .) 1)).Count }}
      {{ end }}
      {{ $span := sub $max $min }}
      {{ range $terms }}
        {{ $size := 1.4 }}
        {{ if gt $span 0 }}
          {{ $size = add 0.8 (mul (div (float (sub .Count $min)) (float $span)) 1.2) }}
        {{ end }}
        <a href="{{ .Page.RelPermalink }}"
           class="tag-cloud__item no-underline"
           style="font-size:{{ printf "%.2f" $size }}rem">
          {{ .Page.LinkTitle }}<span class="tag-cloud__count">{{ .Count }}</span>
        </a>
      {{ end }}
    </div>
```

右侧 `<div class="flex-1">…</div>` 文章网格 + 分页 + zenMode 分支 **保持不动**。

- [ ] **Step 3: 起 dev server 验证渲染**

Run: `cd /Users/ethanhu/Documents/self/my_blog && hugo server -D --bind 0.0.0.0`(后台)
打开 http://localhost:1313/tags/ —— 左侧应为标签云:高频字大、低频字小,每标签后带计数数字;点标签进单标签页正常;右侧文章网格仍在。
(此时样式未加,字号已生效但容器/间距/配色待 Task 7。)

- [ ] **Step 4: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add layouts/_default/terms.html
git commit -m "feat: /tags/ 总览页左侧改频次标签云(字号按文章数+计数)"
```

---

## Task 7: `custom.css` 标签云 + 文章内 badge 样式

**Files:**
- Modify: `static/css/custom.css`

**Interfaces:**
- Consumes:`.tag-cloud` / `.tag-cloud__item` / `.tag-cloud__count`(Task 6);文章内主题 `.badge`。

- [ ] **Step 1: 追加纯 CSS(文件末尾)**

在 `static/css/custom.css` 末尾追加:

```css
/* ==========================================================================
   标签云(/tags/ 总览页左侧)+ 文章内 badge(子项目 H)
   ========================================================================== */
.tag-cloud {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: .5rem .75rem;
    line-height: 1.6;
}
.tag-cloud__item {
    display: inline-flex;
    align-items: baseline;
    padding: .15em .5em;
    border-radius: 6px;
    border: 1px solid rgba(127, 127, 127, .25);
    color: inherit;
    transition: background .15s, border-color .15s, color .15s;
}
.tag-cloud__item:hover {
    background: rgba(127, 127, 127, .12);
    border-color: rgba(127, 127, 127, .5);
}
.tag-cloud__count {
    margin-left: .35em;
    font-size: .7em;
    opacity: .55;
}

/* 文章内标签 badge 微调(复用主题 .badge) */
#dream-single-post-content .badge,
.prose .badge {
    border-radius: 6px;
    transition: background .15s, color .15s;
}
```

- [ ] **Step 2: 验证(dev server 应已 livereload)**

打开 http://localhost:1313/tags/ —— 标签云项有描边/圆角、hover 提亮、计数弱化;窄屏 wrap 不溢出。
打开任一文章 —— 底部/顶部标签 badge 圆角/hover 观感改善。
> 注:custom.css 走裸路径 `/css/custom.css`(无指纹),改后浏览器可能缓存,**硬刷 Cmd+Shift+R** 一次确认。

- [ ] **Step 3: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add static/css/custom.css
git commit -m "feat: 标签云样式 + 文章内 badge 观感微调"
```

---

## Task 8: 全量验证 + 收尾

**Files:** 无(验证)

- [ ] **Step 1: 停 server 后跑生产构建**

Run: `cd /Users/ethanhu/Documents/self/my_blog && pkill -f 'hugo server'; sleep 1; rm -rf public && hugo 2>&1 | grep -iE 'error|warn'`
Expected: 仅 `.Site.LanguageCode` 既有 WARN,无 ERROR、无新增警告。

- [ ] **Step 2: 验证标签链接无 404**

Run: `cd /Users/ethanhu/Documents/self/my_blog && ls public/tags/ | head -20 && echo "标签页目录数:" && ls public/tags/ | wc -l`
Expected: 标签页目录数约等于精简后标签数(40–60 量级,含分页等)。

- [ ] **Step 3: 过 spec「验证方式」清单**

逐项确认:① audit 标签数 40–60;② git diff 只动 tags;③ 构建无错;④ /tags/ 频次云 + 计数 + 点击正常 + 右网格保留;⑤ 文章 badge 观感 + 窄屏;⑥ 标签链接无 404。

- [ ] **Step 4: 确认未误提交 3dgenai**

Run: `cd /Users/ethanhu/Documents/self/my_blog && git status --short`
Expected: 仅 `?? content/posts/3dgenai/`。

---

## 回滚

- 数据:`git checkout <分支> -- content/posts/`。
- 模板:`rm layouts/_default/terms.html`(回落主题原版)。
- 样式:`git checkout <分支> -- static/css/custom.css`。
- 工具:`rm retag.py tag-map.json tests/test_retag.py`。

## 后续子项目
- **I**:about 页重做(#7)。
- 原始 B(图片)/C(3dgenai 重构)/D(工程清理)待排期。
