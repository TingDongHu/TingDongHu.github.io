# about 页重做(子项目 I)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 about 从 flip 翻转卡片改为独立 `/about/` 页:顶部个人叙述 + 五区块(技术栈/最近在学/工具装备/好物橱窗/兴趣爱好)+ 社交链接,导航与头像改为跳转该页。

**Architecture:** 单个 `content/about/index.md`(叙述走正文 markdown,结构化数据走 front matter 数组)+ 自定义模板 `layouts/about/single.html`(define main,渲染叙述 `.Content` + 各区块)+ 覆盖 `renderNavItem.html`/`nav.html` 改 flip 为跳转 + `static/css/custom.css` 纯 CSS(跟随主题明暗)。

**Tech Stack:** Hugo extended、Go template、YAML front matter、CSS、DaisyUI/Tailwind(已编译,新样式走 custom.css 纯 CSS)。

## Global Constraints

- Tailwind 预编译铁律(CLAUDE.md):新 utility 类失效;字号/布局用内联 style(动态)或 `static/css/custom.css` 纯 CSS(稳定类名 `.about-*`)。
- 不改 `themes/dream-3.13.0/`,仅在 `layouts/` 覆盖。
- about 页配色**跟随主题明暗**(emerald/forest),不恒深(区别于代码块)。
- 内容:单 `content/about/index.md`,叙述段走正文,techStack/now/uses/gear/hobby/socials 走 front matter。删旧子页 me/hobby/now/uses/uses1。
- 社交链接 URL、好物橱窗图片/链接由用户后续提供,先留结构 + 占位(空值不渲染)。
- LF 行尾。
- 分支:新建 `feature/about`(基于 main;若 D 已合可基于其后)。
- **不提交** `content/posts/3dgenai/`(未跟踪)。

---

## 文件结构

| 文件 | 责任 | 动作 |
|------|------|------|
| `content/about/index.md` | about 单页:front matter 结构化数据 + 正文叙述 | 重写(原为 headless 标记) |
| `content/about/{me,hobby,now,uses,uses1}.md` | 旧子页 | 删除 |
| `layouts/about/single.html` | 独立 about 页模板 | Create |
| `layouts/partials/renderNavItem.html` | 导航 about 项改跳转 | Create(覆盖主题) |
| `layouts/partials/nav.html` | 头像改跳转 /about/ | Create(覆盖主题) |
| `static/css/custom.css` | `.about-*` 样式 | Modify(追加) |

---

## Task 1: 新建分支 + 重写 content/about/index.md

**Files:**
- Create/Modify: `content/about/index.md`

- [ ] **Step 1: 建分支**

Run: `cd /Users/ethanhu/Documents/self/my_blog && git checkout main && git pull origin main && git checkout -b feature/about && git branch --show-current`
Expected: `feature/about`。

- [ ] **Step 2: 写 index.md(front matter 数据 + 正文叙述)**

覆盖 `content/about/index.md` 为(uses 已合并 uses+uses1 去重;叙述段为已定稿文案):

```markdown
---
title: 关于
techStack: ["图形学", "C++", "游戏引擎", "LLM", "Agent", "3D生成", "机器学习", "排序算法"]
now:
  - { icon: "🎨", name: "GAMES104", url: "https://games104.boomingtech.com/", desc: "现代游戏引擎入门" }
  - { icon: "🏔️", name: "Houdini 基础", url: "https://www.sidefx.com/learn/", desc: "程序化建模与特效" }
  - { icon: "🤗", name: "HuggingFace", url: "https://huggingface.co/learn", desc: "探索开源 AI 社区" }
  - { icon: "🤖", name: "HelloAgent", url: "https://github.com/datawhalechina/hello-agents", desc: "优秀的 Agent 入门项目" }
uses:
  - { name: "Typora", url: "https://typora.io/", desc: "Markdown 极简编辑器" }
  - { name: "Notion", url: "https://www.notion.so/", desc: "一站式笔记工作空间" }
  - { name: "N8N", url: "https://n8n.io/", desc: "节点编排工作流工具" }
  - { name: "Gemini", url: "https://gemini.google.com/", desc: "智能 AI 助手" }
  - { name: "ImageToUrl", url: "https://www.imagetourl.org/", desc: "图片转 URL" }
  - { name: "Anyconv", url: "https://anyconv.com/", desc: "在线文件格式转换" }
  - { name: "PDF24", url: "https://www.pdf24.org/zh/", desc: "PDF 在线工具箱" }
  - { name: "Mixamo", url: "https://www.mixamo.com/", desc: "免费角色动画库" }
  - { name: "Unlock-Music", url: "https://unlock-music.guole.fun/", desc: "音频格式解密" }
  - { name: "EasyGIF", url: "https://easygif.cn/", desc: "在线 GIF 制作" }
  - { name: "Alphacoders", url: "https://alphacoders.com/", desc: "高清壁纸社区" }
  - { name: "哲风壁纸", url: "https://haowallpaper.com/wallpaperForum", desc: "中文壁纸社区" }
  - { name: "爱给网", url: "https://www.aigei.com/", desc: "免费媒体素材" }
  - { name: "模之屋", url: "https://www.aplaybox.com/", desc: "3D 模型社区" }
gear:
  - { name: "", desc: "", img: "", url: "" }
hobby:
  - "🚀 喜欢折腾各种好玩的应用技术"
  - "📸 业余摄影爱好者"
  - "🎮 各类游戏玩家"
  - "💻 数码产品折腾爱好者"
  - "📚 正在阅读:《人类简史》"
  - "🎬 经典重温:《爱乐之城》"
socials:
  - { name: "GitHub", url: "", icon: "logo-github" }
---

我是 **古月月仔**(Shimizu Tou / Ethan Hu),一名计算机在校生。山西平遥人,现居上海。

兴趣多栖——既折腾**计算机图形学**与**游戏引擎**,也一头扎进 **LLM / Agent** 和 **3D 生成**。喜欢上手拆解新工具、新技术,是个停不下来的折腾型选手。

这个博客是我的**个人复习与笔记库**:把学过的东西写清楚、留个档,顺便公开分享给同样在路上的同好。
```

- [ ] **Step 3: 删旧子页**

Run: `cd /Users/ethanhu/Documents/self/my_blog && git rm content/about/me.md content/about/hobby.md content/about/now.md content/about/uses.md content/about/uses1.md`
Expected: 5 个 rm 行。

- [ ] **Step 4: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add content/about/index.md
git commit -q -m "feat: about内容改单页(叙述+techStack/now/uses/gear/hobby/socials),删旧5子页"
```

---

## Task 2: `layouts/about/single.html` 模板

**Files:**
- Create: `layouts/about/single.html`

**Interfaces:**
- Consumes: `content/about/index.md` 的 `.Content` + `.Params.{techStack,now,uses,gear,hobby,socials}`(Task 1)。
- Produces: DOM 类 `.about-hero` / `.about-grid` / `.about-section` / `.about-section__title` / `.about-tag` / `.about-link` / `.about-gear` / `.about-gear__card` / `.about-socials`(供 Task 4 CSS)。

- [ ] **Step 1: 写模板**

创建 `layouts/about/single.html`:

```go-html-template
{{ define "title" }}{{ .Title }}{{ end }}

{{ define "main" }}
<div class="about-page px-4 mt-6 mx-auto">

  <section class="about-hero prose dark:prose-invert">
    {{ .Content | emojify }}
  </section>

  <div class="about-grid mt-8">

    {{ with .Params.techStack }}
    <section class="about-section">
      <h2 class="about-section__title">🧰 技术栈 / 领域</h2>
      <div class="about-tags">
        {{ range . }}<span class="about-tag">{{ . }}</span>{{ end }}
      </div>
    </section>
    {{ end }}

    {{ with .Params.now }}
    <section class="about-section">
      <h2 class="about-section__title">📖 最近在学</h2>
      <ul class="about-links">
        {{ range . }}
        <li><a class="about-link" href="{{ .url }}" target="_blank" rel="noopener">
          <span class="about-link__icon">{{ .icon }}</span>
          <span class="about-link__name">{{ .name }}</span>
          <span class="about-link__desc">{{ .desc }}</span>
        </a></li>
        {{ end }}
      </ul>
    </section>
    {{ end }}

    {{ with .Params.uses }}
    <section class="about-section">
      <h2 class="about-section__title">⚙️ 工具装备</h2>
      <ul class="about-links">
        {{ range . }}
        <li><a class="about-link" href="{{ .url }}" target="_blank" rel="noopener">
          <span class="about-link__name">{{ .name }}</span>
          <span class="about-link__desc">{{ .desc }}</span>
        </a></li>
        {{ end }}
      </ul>
    </section>
    {{ end }}

    {{ $gear := where .Params.gear "name" "!=" "" }}
    {{ with $gear }}
    <section class="about-section about-section--wide">
      <h2 class="about-section__title">🛍️ 好物橱窗</h2>
      <div class="about-gear">
        {{ range . }}
        <a class="about-gear__card" {{ with .url }}href="{{ . }}" target="_blank" rel="noopener"{{ end }}>
          {{ $name := .name }}
          {{ with .img }}<img class="about-gear__img" src="{{ . }}" alt="{{ $name }}" loading="lazy" />{{ end }}
          <div class="about-gear__name">{{ .name }}</div>
          <div class="about-gear__desc">{{ .desc }}</div>
        </a>
        {{ end }}
      </div>
    </section>
    {{ end }}

    {{ with .Params.hobby }}
    <section class="about-section">
      <h2 class="about-section__title">🎮 兴趣爱好</h2>
      <ul class="about-hobby">
        {{ range . }}<li>{{ . | emojify }}</li>{{ end }}
      </ul>
    </section>
    {{ end }}

    {{ $socials := where .Params.socials "url" "!=" "" }}
    {{ with $socials }}
    <section class="about-section">
      <h2 class="about-section__title">🔗 找到我</h2>
      <div class="about-socials">
        {{ range . }}
        <a class="about-social" href="{{ .url }}" target="_blank" rel="noopener" title="{{ .name }}">
          {{ with .icon }}<ion-icon name="{{ . }}"></ion-icon>{{ end }}
          <span>{{ .name }}</span>
        </a>
        {{ end }}
      </div>
    </section>
    {{ end }}

  </div>
</div>
{{ end }}
```

注:`where .Params.gear "name" "!=" ""` 过滤掉占位空条目;socials 同理按 url 过滤。gear img 的 alt:`.img` 的 `with` 块内 `.` 已变为 img 字符串,故先用 `{{ $name := .name }}` 在 with 外存当前项名,再在 alt 引用(模板已如此写)。

- [ ] **Step 2: 起 dev server 验证渲染**

Run: `cd /Users/ethanhu/Documents/self/my_blog && hugo server -D --bind 0.0.0.0`(后台)
打开 http://localhost:1313/about/ —— 应见:叙述段 + 技术栈 pill + 最近在学/工具装备链接列表 + 兴趣爱好 + 找到我(GitHub url 空则该区块不显);好物橱窗因 name 空而整区不显。无样式(Task 4 加)。

Run 验证 HTML: `curl -s http://localhost:1313/about/ | grep -c 'about-section'`
Expected: 区块数(techStack/now/uses/hobby = 4;gear/socials 空不渲染)。

- [ ] **Step 3: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add layouts/about/single.html
git commit -q -m "feat: about独立页模板(叙述+5区块+社交,front matter驱动)"
```

---

## Task 3: 导航 + 头像改跳 /about/

**Files:**
- Create: `layouts/partials/renderNavItem.html`(覆盖主题)
- Create: `layouts/partials/nav.html`(覆盖主题)

- [ ] **Step 1: 复制主题 renderNavItem.html 为覆盖基底**

Run: `cd /Users/ethanhu/Documents/self/my_blog && cp themes/dream-3.13.0/layouts/partials/renderNavItem.html layouts/partials/renderNavItem.html`

- [ ] **Step 2: 改 about 分支为跳转链接**

把 `layouts/partials/renderNavItem.html` 开头的 about 分支:

```go-html-template
{{ if and (eq . "about") }}

{{ with site.GetPage "/about" }}
{{ $aboutPages := .Resources.ByType "page" }}
{{ if gt (len $aboutPages) 0 }}
<div role="link" tabindex="0" class="text-sm font-semibold cursor-pointer hover:underline" @click="flip = !flip" title="{{ T "about" }}">
  {{- T "about" -}}
</div>
{{ end }}
{{ end }}

{{ else if ...
```

替换为(改 flip 为 `<a href>`,且不再依赖子页 Resources):

```go-html-template
{{ if and (eq . "about") }}
{{ with site.GetPage "/about" }}
<a class="text-sm font-semibold cursor-pointer hover:underline" href="{{ .RelPermalink }}" title="{{ T "about" }}">
  {{- T "about" -}}
</a>
{{ end }}
{{ else if ...
```

(其余 search/rss/posts/categories/tags/else 分支保持不动。)

- [ ] **Step 3: 复制主题 nav.html 为覆盖基底**

Run: `cd /Users/ethanhu/Documents/self/my_blog && cp themes/dream-3.13.0/layouts/partials/nav.html layouts/partials/nav.html`

- [ ] **Step 4: 改头像为跳转 /about/**

把 `layouts/partials/nav.html` 的头像块:

```go-html-template
      <div class="avatar cursor-pointer hover:avatar-online" @click="flip = !flip" title="{{ T "flip" }}">
        <div class="h-10 rounded-full">
          <img src="{{ site.Params.avatar | relURL }}" alt="{{ site.Params.headerTitle }}" />
        </div>
      </div>
```

替换为(包一层链接,保留 avatar 样式):

```go-html-template
      <a class="avatar cursor-pointer hover:avatar-online" href="{{ "/about/" | relLangURL }}" title="{{ T "about" }}">
        <div class="h-10 rounded-full">
          <img src="{{ site.Params.avatar | relURL }}" alt="{{ site.Params.headerTitle }}" />
        </div>
      </a>
```

(其余 nav 逻辑不动。)

- [ ] **Step 5: dev server 验证跳转(无 flip)**

打开 http://localhost:1313/ —— 点导航 "about" 文字 → 跳 `/about/`(不翻转);点左上头像 → 跳 `/about/`。首页正常无报错。

Run: `cd /Users/ethanhu/Documents/self/my_blog && curl -s http://localhost:1313/ | grep -oE 'href="[^"]*about[^"]*"' | head`
Expected: 出现指向 `/about/` 的 href(导航 + 头像)。

- [ ] **Step 6: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add layouts/partials/renderNavItem.html layouts/partials/nav.html
git commit -q -m "feat: about导航与头像从flip翻转改为跳转独立/about/页"
```

---

## Task 4: `custom.css` 样式

**Files:**
- Modify: `static/css/custom.css`

**Interfaces:**
- Consumes: Task 2 的 `.about-*` 类。

- [ ] **Step 1: 追加纯 CSS(文件末尾)**

在 `static/css/custom.css` 末尾追加:

```css
/* ==========================================================================
   about 页(子项目 I)— 独立页,跟随主题明暗
   ========================================================================== */
.about-page {
    max-width: 60rem;
}
.about-hero {
    max-width: 100%;
}
.about-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
}
@media (min-width: 1024px) {
    .about-grid { grid-template-columns: 1fr 1fr; }
    .about-section--wide { grid-column: 1 / -1; }
}
.about-section {
    border: 1px solid rgba(127, 127, 127, .2);
    border-radius: 12px;
    padding: 1.1rem 1.25rem;
    background: rgba(127, 127, 127, .04);
}
.about-section__title {
    font-size: 1.05rem;
    font-weight: 700;
    margin: 0 0 .75rem;
}
/* 技术栈 pill */
.about-tags { display: flex; flex-wrap: wrap; gap: .5rem; }
.about-tag {
    font-size: .85rem;
    padding: .2em .7em;
    border-radius: 999px;
    border: 1px solid rgba(127, 127, 127, .3);
    background: rgba(127, 127, 127, .08);
}
/* 链接列表(now/uses) */
.about-links { list-style: none; padding: 0; margin: 0; }
.about-links li { margin: .35rem 0; }
.about-link {
    display: flex;
    align-items: baseline;
    gap: .5rem;
    padding: .3rem .5rem;
    border-radius: 6px;
    text-decoration: none;
    color: inherit;
    transition: background .15s;
}
.about-link:hover { background: rgba(127, 127, 127, .12); }
.about-link__name { font-weight: 600; }
.about-link__desc { font-size: .8rem; opacity: .65; }
/* 好物橱窗 */
.about-gear {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
    gap: 1rem;
}
.about-gear__card {
    display: block;
    border: 1px solid rgba(127, 127, 127, .2);
    border-radius: 10px;
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    transition: transform .15s, box-shadow .15s;
}
.about-gear__card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, .12);
}
.about-gear__img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    display: block;
}
.about-gear__name { font-weight: 600; padding: .5rem .6rem 0; }
.about-gear__desc { font-size: .8rem; opacity: .65; padding: 0 .6rem .6rem; }
/* 兴趣爱好 */
.about-hobby { list-style: none; padding: 0; margin: 0; }
.about-hobby li { margin: .3rem 0; }
/* 社交 */
.about-socials { display: flex; flex-wrap: wrap; gap: .75rem; }
.about-social {
    display: inline-flex;
    align-items: center;
    gap: .4rem;
    padding: .35rem .75rem;
    border-radius: 8px;
    border: 1px solid rgba(127, 127, 127, .3);
    text-decoration: none;
    color: inherit;
    transition: background .15s;
}
.about-social:hover { background: rgba(127, 127, 127, .12); }
.about-social ion-icon { font-size: 1.1rem; }
```

- [ ] **Step 2: 验证(dev server livereload,硬刷)**

打开 http://localhost:1313/about/(Cmd+Shift+R,custom.css 裸路径可能缓存)—— 叙述段舒适宽度;区块卡片描边/圆角;技术栈 pill;now/uses 链接 hover 高亮;兴趣爱好列表;窄屏单列。明暗切换配色跟随(描边/背景用半透明灰,自动适配)。

- [ ] **Step 3: 提交**

```bash
cd /Users/ethanhu/Documents/self/my_blog
git add static/css/custom.css
git commit -q -m "feat: about页样式(.about-* 纯CSS,响应式网格,跟随主题明暗)"
```

---

## Task 5: 全量验证

**Files:** 无(验证)

- [ ] **Step 1: 停 server,生产构建**

Run: `cd /Users/ethanhu/Documents/self/my_blog && pkill -f 'hugo server'; sleep 1; rm -rf public && hugo 2>&1 | grep -iE 'error|warn'; echo done`
Expected: 仅 `.Site.LanguageCode` 既有 WARN,无 ERROR。

- [ ] **Step 2: /about/ 独立页生成**

Run: `cd /Users/ethanhu/Documents/self/my_blog && test -f public/about/index.html && grep -c 'about-section' public/about/index.html`
Expected: 文件存在;about-section 数 = 4(techStack/now/uses/hobby;gear/socials 空不渲染)。

- [ ] **Step 3: 旧子页无残留 404**

Run: `cd /Users/ethanhu/Documents/self/my_blog && ls public/about/ && grep -rl '/about/me\|/about/now\|/about/uses' public/ | head`
Expected: about/ 下无 me/now/uses 等独立页目录;无站内链接指向旧子页。

- [ ] **Step 4: 过 spec 验证清单**

逐项:① 构建无错;② /about/ 独立页五区块(空块不显);③ 导航/头像跳转非 flip;④ pill/链接/橱窗/hobby/社交渲染;⑤ 明暗跟随;⑥ 窄屏单列;⑦ 旧子页无 404;⑧ 首页正常。

- [ ] **Step 5: 确认未误提交 3dgenai**

Run: `cd /Users/ethanhu/Documents/self/my_blog && git status --short`
Expected: 仅 `?? content/posts/3dgenai/`(及可能的大写幻影残留,若 D 未先合)。

---

## 回滚

- `git checkout main -- content/about/ layouts/partials/renderNavItem.html layouts/partials/nav.html static/css/custom.css`
- `rm layouts/about/single.html`
- 整分支:`git checkout main && git branch -D feature/about`

## 待用户补充
- 社交链接 URL(GitHub 等):填 `content/about/index.md` 的 `socials[].url`。
- 好物橱窗:填 `gear` 数组(name/desc/img/url);图片放 `static/img/` 或 `content/about/` 同级。

## 后续子项目
- **D**:工程清理(独立计划 2026-06-25-cleanup.md)。
- **C**:3dgenai 重构。**B**:图片优化。
